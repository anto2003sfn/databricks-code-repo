resources:
  jobs:
    retail_etl_scheduling_pipeline:
      name: retail_etl_scheduling_pipeline
      tasks:
        - task_key: retail_actuals
          pipeline_task:
            pipeline_id: c17f425b-89fb-4742-a039-59f0e7229c8d
      queue:
        enabled: true
      parameters:
        - name: catalog
          default: "${var.catalog}"
        - name: schema
          default: "${var.schema}"
      performance_target: PERFORMANCE_OPTIMIZED
